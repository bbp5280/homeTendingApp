export const addHouses = (houses) => ({
  type: 'ADD_HOUSES',
  houses
});

export const addMessages = (messages) => ({
  type: 'ADD_MESSAGES',
  messages
});

export const addWeather = (weather) => ({
  type: 'ADD_WEATHER',
  weather
});
